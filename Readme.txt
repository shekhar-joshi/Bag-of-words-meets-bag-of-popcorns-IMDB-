1.This project requires Python 3 or more, and the following Python libraries installed:
    NumPy
    Beautiful Soup
    nltk
    scikit-learn
    re

2.You will also need to have software installed:
    Anaconda
	-spyder

3.In a terminal or command window,
 	type anaconda-navigator
		in the Navigator open spyder

4.In spyder goto the right corner for the directory and change it to the datasets existed
